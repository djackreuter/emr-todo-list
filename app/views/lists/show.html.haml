.row
  .col-lg-6
    .tasks-card.card
      %p
        = @list.name
      %ol
        - @list.tasks.each do |task|
          %li
            = task.task
            = link_to "Done", list_task_path(task.list, task.id), method: :delete, class: "float-right"

= link_to "Back", lists_path
= '|'
= link_to "Edit", edit_list_path